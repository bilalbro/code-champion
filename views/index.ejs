<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <%- include('css.ejs')  %>
   <title>Project 4</title>
</head>
<body class="bg-light p-md-5">
   <div class="container p-4" style="max-width: 600px;">
      <h1>CodeChampion 2022</h1>
      <hr>
      <div class="">
         <p>Hello and welcome to CodeChampion 2022!</p>
         <p>There are 3 different competitions for three of the most popular programming languages in the world right now: <b>Python</b>, <b>C++</b>, <b>Java</b>.</p>
         <p>If you are interested in taking part in this competition and getting informed about each of the latest updates in CodeChampion 2022, please sign up using the form below.</p>
      </div>
      <form method="post" class="container-fluid bg-white p-4 shadow mt-5 rounded-3">
         <h1 class="h5">Sign up</h1>
         <% if (typeof errorMessageList !== 'undefined') { %>
            <div class="alert alert-danger mt-4">
               <h6>Errors:</h6>
               <ul class="mb-0">
                  <% errorMessageList.forEach(errorMessage => { %>
                     <li><%= errorMessage %></li>
                  <% }) %>
               </ul>
            </div>
         <% } %>
         <div class="form-group mt-4">
            <label class="form-label">Name</label>
            <input class="form-control" name="name" value="<%= typeof userInput !== 'undefined' ? userInput.name : '' %>" type="text" autocomplete="off">
         </div>
         <div class="form-group mt-4">
            <label class="form-label">Email address</label>
            <input class="form-control" autocomplete="off" name="email" value="<%= typeof userInput !== 'undefined' ? userInput.email : '' %>"  type="text">
         </div>
         <div class="form-group mt-4">
            <p>Contests you wish to participate in:</p>
            <div class="form-group">
               <input class="input-checkbox" type="checkbox" id="i1" <%= typeof userInput !== 'undefined' ? (userInput.competitions?.includes('Python') ? 'checked' : '') : '' %> value="Python" name="competitions">
               <label for="i1">Python</label>
            </div>
            <div class="form-group">
               <input class="input-checkbox" type="checkbox" id="i2" <%= typeof userInput !== 'undefined' ? (userInput.competitions?.includes('C++') ? 'checked' : '') : '' %>  value="C++" name="competitions">
               <label for="i2">C++</label>
            </div>
            <div class="form-group">
               <input class="input-checkbox" type="checkbox" id="i3" <%= typeof userInput !== 'undefined' ? (userInput.competitions?.includes('Java') ? 'checked' : '') : '' %> value="Java" name="competitions">
               <label for="i3">Java</label>
            </div>
         </div>
         <div class="form-group mt-4">
            <button class="btn btn-warning">Submit</button>
         </div>
      </form>
   </div>  
</body>
</html>